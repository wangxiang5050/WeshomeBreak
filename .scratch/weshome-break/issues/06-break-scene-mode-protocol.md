# 06 — 展示模式协议 + 注册/选择

**What to build:** 建立可插拔的休息展示模式协议与注册/选择逻辑。端到端可验证：遮罩能按「固定选择」或「不重复随机」策略选出一种模式并展示其视图；当前可只注册一个占位模式，真正的 Staff Melody Scene 由后续 issue 接入。

**Blocked by:** 04 — 全屏休息遮罩(多屏覆盖 + 占位视觉 + 倒计时数字)

**Status:** done

- [x] `GrillBreakCore` 中定义 `BreakSceneMode` 协议(或等价命名),每种模式提供自身的展示视图与元信息
- [x] 实现模式注册表,支持注册多个模式(本 issue 可只注册一个占位模式以打通链路)
- [x] 实现「固定选择」逻辑:按用户指定的模式返回固定结果
- [x] 实现「不重复随机」逻辑:随机选择时排除上一次选中的模式;附单元测试验证多模式场景下不会连续两次选中同一模式
- [x] 休息遮罩通过协议渲染当前选中模式的视图(占位即可),角落倒计时保留
