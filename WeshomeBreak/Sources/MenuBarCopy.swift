import Foundation

/// Shared menu-bar copy so the SwiftUI menu and the AppKit live-title
/// updater stay fingerprint-matched.
enum MenuBarCopy {
    static let pause = "暂停"
    static let resume = "继续"
    static let startBreakNow = "立即开始一次休息"
    static let settings = "设置…"
    static let quit = "退出 App"
}
