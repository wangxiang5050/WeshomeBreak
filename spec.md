# Spec: Weshome Break — macOS 站立/休息提醒应用

**Triage label:** `ready-for-agent`

## Problem Statement

长时间在 Mac 前工作,容易忘记按时站立、休息,损害健康。用户需要一个原生的 macOS 应用,能够按照自己设定的工作/休息节奏,在到点时用足够强的方式提醒自己休息,而不是被一条容易忽略的系统通知带过。同时,休息提醒的视觉呈现应该是有吸引力、可持续迭代的体验,而不是一块枯燥的倒计时页面。

## Solution

开发一个纯菜单栏(Menu Bar)原生 macOS App「Weshome Break」。App 按照可配置的工作时长/休息时长循环运行,工作时长结束后,在所有已连接的屏幕上弹出全屏遮罩强制打断用户,遮罩中央以「音符沿五线谱从四周飘入汇聚」的动画方式展示休息倒计时。用户可以在遮罩上(鼠标移动时浮现的控制条)选择跳过本次休息或延迟 5 分钟(可配置),也可以在菜单栏中随时暂停/继续整体计时、手动触发一次休息、打开设置面板调整参数。

展示效果(如五线谱音符)通过统一协议设计为可插拔模式,当前只实现一种,后续可以持续增加新的展示效果并纳入随机轮换池,不影响主流程代码。

## User Stories

1. As a Mac 用户, I want 一个菜单栏常驻的提醒工具, so that 我不需要额外打开一个独立窗口或占用 Dock 空间。
2. As a Mac 用户, I want 可以自定义工作时长和休息时长, so that 我可以按照自己的作息习惯设置节奏(默认工作 20 分钟/休息 5 分钟)。
3. As a Mac 用户, I want 休息到点后屏幕被全屏遮罩强制覆盖, so that 我不会因为忙于工作而无视一条容易被忽略的系统通知。
4. As a 多显示器用户, I want 休息遮罩同时覆盖我所有连接的显示器, so that 我不会因为切换到副屏而绕过休息提醒。
5. As a 正在演示/开会的用户, I want App 检测到有全屏应用在前台或系统开启了勿扰/专注模式时自动推迟本次休息提醒, so that 我的演示或会议不会被意外打断。
6. As a Mac 用户, I want 被推迟的休息提醒在条件解除后尽快补上, so that 我不会因为一次会议而彻底错过这次休息。
7. As a 临时有紧急任务的用户, I want 在遮罩上可以选择「跳过本次休息」, so that 我可以在必要时立刻回到工作中。
8. As a 想再多做一会儿的用户, I want 在遮罩上可以选择「延迟休息」(默认5分钟,可配置), so that 我可以给自己一小段缓冲时间收尾当前工作。
9. As a 用户, I want 「跳过」后下一次休息按重新计满一个完整工作时长来算, so that 我不会因为跳过而导致休息节奏被永久打乱成很短的间隔。
10. As a 用户, I want 可以多次连续点击「延迟」, so that 我能按自己的节奏灵活推后休息,而不会被系统强行限制次数。
11. As a 用户, I want 遮罩上的操作按钮默认隐藏、只在鼠标移动时短暂浮现, so that 休息期间的视觉画面保持纯净不被按钮干扰。
12. As a 用户, I want 休息期间看到音符从屏幕四周持续飘入并汇聚到中央五线谱上排列成乐句, so that 休息过程本身是一种放松、有美感的体验,而不是枯燥地等待倒计时。
13. As a 用户, I want 音符的音高在一个简单音阶(如 C 大调)内随机分布, so that 视觉上呈现出的乐句更接近真实乐谱,而不是完全杂乱的随机点。
14. As a 用户, I want 遮罩画面采用深色夜间氛围配色(深色背景 + 浅色线条/音符), so that 整体视觉风格宁静、契合放松休息的场景。
15. As a 用户, I want 在遮罩角落看到剩余休息时间的倒计时数字, so that 我可以掌握还需要休息多久,不必完全依赖动画节奏。
16. As a 想尝试更多展示效果的用户, I want 系统在未来支持新增多种休息展示模式(不止五线谱音符), so that 我可以体验更丰富的休息视觉效果。
17. As a 用户, I want 在设置中选择「固定使用某个展示模式」或「随机轮换」, so that 我可以按自己的喜好决定休息时看到的效果是否固定不变。
18. As a 选择了随机轮换的用户, I want 每次随机选择时排除上一次用过的模式(不重复随机), so that 我不会连续两次看到完全相同的展示效果。
19. As a 用户, I want 菜单栏菜单里有「立即开始一次休息」的入口, so that 我可以在测试效果或临时想休息时主动触发,而不必等待计时器走完。
20. As a 用户, I want 菜单栏菜单里有「暂停/继续」整体计时的开关, so that 我可以在离开电脑或开会期间临时冻结计时,不产生错误的休息提醒。
21. As a 用户, I want 菜单栏菜单里有「退出 App」的入口, so that 我可以随时完全关闭这个工具。
22. As a 用户, I want App 默认在登录时自动启动, so that 我不需要每次开机都手动打开它才能生效。
23. As a 用户, I want 可以在设置中关闭「登录时启动」这个选项, so that 如果我不想让它自动启动,可以自行控制。
24. As a 用户, I want 我调整过的所有设置项(工作/休息时长、展示模式选择、跳过/延迟开关及延迟时长、登录启动开关)在重启 App 后依然保留, so that 我不需要每次都重新配置一遍。
25. As a 用户, I want App 重启后当前这一轮工作计时从 0 开始(不做跨进程计时状态持久化), so that 行为简单可预期,不会因为奇怪的历史状态导致提前或延后触发休息。
26. As a 关注代码质量的开发者, I want 核心调度逻辑与 UI 解耦,放在独立的 Swift Package 中并有单元测试覆盖, so that 未来修改或扩展逻辑时可以快速验证正确性,不需要每次手动跑 UI 回归。

## Implementation Decisions

**总体架构**
- macOS 原生 App,纯菜单栏形态,无 Dock 图标、无独立主窗口;菜单栏交互使用 SwiftUI `MenuBarExtra`。
- Bundle ID:`com.weshome.break`;App 名称:Weshome Break;界面文案统一使用中文。
- 最低支持 macOS 26+,以 SwiftUI 为主要 UI 框架。
- 核心业务逻辑拆分为一个本地 Swift Package(`GrillBreakCore`),与 App target(UI 与系统集成)解耦。`GrillBreakCore` 不依赖 SwiftUI/AppKit,只包含纯逻辑与协议定义。

**调度模型**
- 当前版本只实现「简单循环」调度策略:工作时长(默认 20 分钟)结束后触发一次休息,休息时长默认 5 分钟,循环往复。
- 调度逻辑通过策略/协议方式设计,为后续新增其他调度模型(如固定工作时段模式、番茄钟预设组合)留出扩展点,新增策略不应改动现有调用方代码。
- App 需要能判断当前是否有全屏应用在前台运行,以及系统是否处于勿扰/专注模式;若命中任一条件,本次休息提醒推迟触发,并在条件解除后尽快重新触发(不是静默跳过、不丢失这次休息)。

**休息遮罩与展示模式**
- 休息触发时,在所有当前连接的屏幕上各创建一个全屏遮罩窗口,窗口层级需高于常规应用(用于强制置顶盖住 Dock 与菜单栏),遮罩内容使用 SwiftUI View 承载,窗口本身用 AppKit(`NSWindow`)管理以精确控制多屏覆盖与层级行为。
- 展示模式通过统一协议(`BreakSceneMode` 或等价命名)定义,每种模式提供自己的 SwiftUI 视图与展示所需的元信息;新增模式只需实现该协议并注册,无需修改遮罩窗口或调度器的调用逻辑。
- 当前实现一种展示模式——「五线谱音符」:
  - 标准五线谱(5 条横线)居中显示;音符从屏幕四周以随机方向、随机时间间隔持续飘入,汇聚到中央谱线上排列成一段乐句。
  - 音符音高在一个简单音阶(如 C 大调音阶)范围内随机取值,而非完全自由的随机高度,视觉上更接近真实乐谱。
  - 整体配色为深色夜间氛围(深色/近黑渐变背景 + 白色或浅金色线条与音符)。
  - 遮罩角落显示剩余休息时间的倒计时数字。
- 设置中提供两种模式选择策略:「固定使用某个模式」或「随机轮换」;随机轮换采用不重复随机(排除上一次使用过的模式)。当前只有一种模式时,该选择项仍需存在于设置结构中,为后续新增模式做好准备。

**遮罩交互(跳过/延迟)**
- 遮罩上提供「跳过本次休息」与「延迟休息」两个独立按钮/开关,默认在设置中都是开启状态,可在设置中分别关闭。
- 按钮默认隐藏,仅在用户鼠标在遮罩窗口上移动时短暂浮现(类似视频播放器控制条交互),隐藏后不干扰展示画面。
- 「跳过」行为:立即关闭本次休息遮罩,并将下一次休息的触发时间重置为「从当前时刻起重新计满一个完整工作时长」。
- 「延迟」行为:关闭本次休息遮罩,在设置中配置的延迟时长(默认 5 分钟,可自定义数值)后重新触发同一次休息;允许用户重复点击延迟,不设置次数上限。

**菜单栏功能**
- 菜单栏点击弹出面板/菜单,包含以下入口:
  - 设置(工作时长、休息时长、展示模式选择策略、跳过/延迟开关及延迟时长配置、登录时启动开关)。
  - 「立即开始一次休息」:手动触发一次休息流程,复用与自动触发相同的遮罩与调度逻辑。
  - 「暂停/继续」:冻结或恢复当前的整体工作/休息计时。
  - 「退出 App」。

**持久化与生命周期**
- 所有设置项使用 `@AppStorage`/`UserDefaults` 持久化,不引入额外的数据库或历史统计存储。
- App 每次启动(包括登录自动启动)都从 0 开始一轮新的工作计时,不持久化、不恢复跨进程的计时中间状态。
- 登录时自动启动使用 `SMAppService` 实现,默认开启,可在设置中关闭。

**分发与签名**
- 关闭 App Sandbox,使用本机 Apple ID 进行签名(Xcode 自动管理签名),不做 notarization 流程;定位为个人自用工具,直接分发(非 App Store 上架)。

## Testing Decisions

- 好的测试只验证外部可观察行为(状态转换的结果、时间到达后触发了什么动作),不测试内部实现细节(比如具体用了哪个 Timer API)。
- 唯一的测试 seam 放在 `GrillBreakCore` 这个纯逻辑 Swift Package 上,不测试 SwiftUI/AppKit 视图层(菜单栏 UI、全屏遮罩窗口渲染),因为这些是 UI 相关、且是全新代码,难以在单元测试中稳定覆盖。
- 需要覆盖测试的核心逻辑包括:
  - 简单循环调度器:给定工作/休息时长,验证在到达对应时间点后正确地在「工作」与「休息」两种状态之间转换。
  - 全屏应用/勿扰模式检测命中时,休息触发被推迟,条件解除后能补触发(可通过对检测结果的抽象/依赖注入来模拟这两种环境状态,不依赖真实系统 API)。
  - 「跳过」逻辑:触发后下一次休息的目标时间被正确重置为「当前时刻 + 完整工作时长」。
  - 「延迟」逻辑:触发后休息重新排定到「当前时刻 + 延迟时长」,且可重复触发不受次数限制。
  - `BreakSceneMode` 注册与选择逻辑:固定模式选择返回指定模式;随机轮换在多个模式下不会连续两次选中同一个模式。
- 测试框架使用 Swift Testing(而非 XCTest),与项目最低支持 macOS 26+ 的技术栈保持一致。
- 项目目前是全新代码库,没有既有测试可作为直接的先例参照;`GrillBreakCore` 的测试目标就是本项目内该类测试的第一批先例。

## Out of Scope

- 提示音、系统通知(Notification banner)辅助提醒——本版本纯视觉全屏遮罩提醒,不做听觉/系统通知层面的双重提示。
- 休息历史统计功能(如“本周休息了几次”)——当前只做设置项持久化,不引入历史数据记录与统计展示。
- 「工作时段模式」「番茄钟预设组合」等除简单循环外的其他调度模型——架构上预留扩展点,但本版本不实现。
- App Store 上架、完整的 notarization 公开分发流程——当前定位为个人自用,直接分发。
- 界面多语言/本地化(如中英双语)——当前只做中文界面。
- 除「五线谱音符」外的其他休息展示模式——架构上做成可插拔协议,但本版本只实现这一种。
- 跨进程/重启后的计时状态持久化与恢复——每次启动都重新开始一轮工作计时。

## Further Notes

- 本 spec 由 `grill-me` 交互式访谈(grilling 会话)整理而成,访谈过程逐轮确认了产品形态、调度模型、遮罩交互、视觉细节、工程结构与分发方式等决策,详见对话历史。
- 项目当前未配置任何外部 issue tracker(未检测到 GitHub/Linear 等集成,也没有 `setup-matt-pocock-skills` 的相关配置),因此本 spec 以本地 `spec.md` 文件形式产出,未发布到外部 issue tracker。若后续接入了 issue tracker,可将本文件内容直接迁移过去并补充 triage 标签。
- 后续实现阶段建议先搭建 `GrillBreakCore` 包与调度器骨架并配好单元测试,再逐步接入 SwiftUI/AppKit 的菜单栏与遮罩窗口 UI,最后实现「五线谱音符」展示视图与动画细节。
